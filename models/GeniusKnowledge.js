class GeniusKnowledge {
  constructor(
    id,
    categoryIds,

    title, 
    imageUrl,

    desc,
    linkURL,
    

  ) {
    this.id = id;
    this.categoryIds = categoryIds;
    this.title = title;
    this.imageUrl = imageUrl;

    this.desc = desc;
    this.linkURL = linkURL;
  }
}
export default GeniusKnowledge;
